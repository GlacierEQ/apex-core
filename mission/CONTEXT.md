# 🏛️ DM MONOLITH — Operational Context Capsule (CONTEXT.md)

**Mission ID:** `APEX-DM-MONOLITH-2026`  
**Standard:** `far-away-party` 5-File Durable State Contract · Epistemic Ladder $\mathcal{L}_0 \to \mathcal{L}_6$  
**Last Updated:** `2026-09-01T09:07:00-10:00`  

---

## 1. Operating Environment Realities ($\mathcal{L}_2$)

- **Host Machine:** macOS Darwin, Apple Silicon / NVMe Monolith locus at `/Users/kcbflux/APEX_SYSTEM`.
- **Primary Agent Interface:** Google Antigravity Agent Runtime (`gemini-3.7-flash-high`, 29 unified MCP servers configured).
- **Secondary Multi-Coder CLIs:** OpenCode Zen (`~/.config/opencode/config.json`), Kilo Code (`~/.config/kilo/kilo.json`).
- **Disk State:** Root disk volume 113GB (~600MB free buffer; strict ephemeral cleanup required for build artifacts).
- **Python / Toolchain:** Python 3.14.7, Rust 1.98 (`~/.cargo/bin`), Go 1.24+, Swift Metal compiler, TableGen / MLIR compiler tools.

---

## 2. Active Repository Mesh

- **Monolith Head:** `/Users/kcbflux/APEX_SYSTEM/INFRASTRUCTURE/apex-core`
- **7 Forged Domain Repositories (Pushed to GitHub `GlacierEQ/*`):**
  1. `apex-raft-forge` (Go 1.27 Raft + TLA+ spec)
  2. `apex-zk-receipt-chain` (Cairo 1.0 + Lean 4 formal invariants)
  3. `apex-ros2-autonomy` (ROS2 Humble autonomy bridge + Gazebo simulation)
  4. `apex-ebpf-sentinel` (eBPF CO-RE Linux security probe + Go cross-platform loader)
  5. `apex-metal-compute` (Metal MSL attention kernels + Swift bridge)
  6. `apex-ir-compiler` (MLIR TableGen dialect + Rust AST frontend)
  7. `apex-crdt-mesh` (Rust Tokio async convergent state mesh)
- **Shared Engines & Matrices:**
  - `Genius-Mastery` (25/25 passing unit tests, impact scoring, lossless migration)
  - `the-tower-of-babel` (40-floor polyglot registry, eBPF advanced to `tested`)
  - `mega-skills` (60 active mega-skills symlinked to agent runtimes)

---

## 3. Epistemic Constraints & Non-Negotiables

1. No claim accepted without $\ge \mathcal{L}_2$ empirical proof receipts in `EVIDENCE.md`.
2. Antigravity is primary; OpenRouter free tiers absorb routine queries; Claude Code is quota-governed.
3. Air-gapped PII scrubbing (`scripts/apex_scrubber.py`) enforced on all telemetry before dispatch.
4. Zero stubs (`pass`, `TODO`, `return None` without logic) in any shipped chapter layer.
